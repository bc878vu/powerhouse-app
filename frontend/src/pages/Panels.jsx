import React, { useCallback, useEffect, useMemo, useState } from "react";
import { useNavigate } from "react-router-dom";
import API from "../firebaseApi";
import factoryMap from "../assets/factory-map.svg";
import { RefreshCw, Plus, Eye, Pencil, Trash2, Zap, Power, Wrench, AlertTriangle, LayoutGrid, Archive, Search, MapPin, X, Info } from "lucide-react";

const STATUS = {
  live: { label: "Live", icon: Zap, color: "text-green-400", bg: "bg-green-500/10", border: "border-green-500/30", dot: "bg-green-400" },
  off: { label: "Off", icon: Power, color: "text-red-400", bg: "bg-red-500/10", border: "border-red-500/30", dot: "bg-red-400" },
  maintenance: { label: "Maintenance", icon: Wrench, color: "text-yellow-400", bg: "bg-yellow-500/10", border: "border-yellow-500/30", dot: "bg-yellow-400" },
  affected: { label: "Affected", icon: AlertTriangle, color: "text-orange-400", bg: "bg-orange-500/10", border: "border-orange-500/30", dot: "bg-orange-400" },
  unknown: { label: "Unknown", icon: AlertTriangle, color: "text-slate-400", bg: "bg-slate-500/10", border: "border-slate-500/30", dot: "bg-slate-400" }
};
const DETAIL_FIELDS = [["Panel Type","panel_type"],["Area","area"],["Location","location"],["Voltage","voltage"],["Rated Current","rated_current"],["Frequency","frequency"],["Phase","phase"],["Incomer Type","incomer_type"],["Incomer Rating","incomer_rating"],["Breaker Type","breaker_type"],["Breaker Rating","breaker_rating"],["Breaking Capacity","breaking_capacity"],["Busbar Rating","busbar_rating"],["Busbar Material","busbar_material"],["Incoming Cable","incoming_cable_size"],["Cable Type","incoming_cable_type"],["Cable Cores","incoming_cable_cores"],["Cable Length","incoming_cable_length"],["Manufacturer","manufacturer"],["Model","model"],["Serial Number","serial_number"],["IP Rating","ip_rating"],["Installation Date","installation_date"],["Short Circuit","short_circuit_rating"],["Insulation Voltage","insulation_voltage"],["Control Voltage","control_voltage"],["Earthing","earthing_details"],["Last Maintenance","last_maintenance_date"],["Next Maintenance","next_maintenance_date"]];
const val = (p,k) => p?.[k] ?? "N/A";

export default function Panels() {
  const navigate = useNavigate();
  const [panels,setPanels] = useState([]), [search,setSearch] = useState(""), [statusFilter,setStatusFilter] = useState("all");
  const [loading,setLoading] = useState(true), [message,setMessage] = useState(""), [updating,setUpdating] = useState(null), [selectedPanel,setSelectedPanel] = useState(null);
  const load = useCallback(async()=>{setLoading(true);try{const r=await API.get("/panels");setPanels(Array.isArray(r.data?.panels)?r.data.panels:[]);setMessage("");}catch(e){console.error(e);setMessage(e.message||"Failed to load panels from Firebase.");}finally{setLoading(false);}},[]);
  useEffect(()=>{load();const u=API.subscribeToPanels?.(items=>setPanels(Array.isArray(items)?items:[]));return()=>u?.();},[load]);
  const statusOf=p=>String(p.effective_status||p.status||"unknown").toLowerCase();
  const filtered=useMemo(()=>{const t=search.trim().toLowerCase();return panels.filter(p=>{const s=statusOf(p), text=[p.panel_code,p.panel_name,p.panel_type,p.area,p.location,p.manufacturer,p.model].filter(Boolean).join(" ").toLowerCase();return(statusFilter==="all"||s===statusFilter)&&(!t||text.includes(t));});},[panels,search,statusFilter]);
  const counts=useMemo(()=>({total:panels.length,live:panels.filter(p=>statusOf(p)==="live").length,off:panels.filter(p=>statusOf(p)==="off").length,maintenance:panels.filter(p=>statusOf(p)==="maintenance").length,affected:panels.filter(p=>statusOf(p)==="affected").length}),[panels]);
  const changeStatus=async(panel,status)=>{if(panel.status===status)return;setUpdating(panel.id);try{await API.put(`/panels/${panel.id}/status`,{status,reason:`Panel changed to ${status}`});setPanels(prev=>prev.map(p=>p.id===panel.id?{...p,status}:p));setSelectedPanel(prev=>prev?.id===panel.id?{...prev,status}:prev);}catch(e){setMessage(e.message||"Could not update panel status.");}finally{setUpdating(null);}};
  const archive=async(panel)=>{if(!window.confirm(`Move ${panel.panel_name||panel.panel_code} to Panel History?`))return;setUpdating(panel.id);try{await API.delete(`/panels/${panel.id}`);setSelectedPanel(null);await load();}catch(e){setMessage(e.message||"Could not archive panel.");}finally{setUpdating(null);}};

  return <div className="space-y-6 animate-in fade-in duration-500 pb-8">
    {message&&<div className="p-4 rounded-2xl bg-red-500/10 border border-red-500/20 text-red-400 font-bold text-sm">{message}</div>}
    <div className="flex flex-col lg:flex-row lg:items-center justify-between gap-4"><div className="flex items-center gap-4"><div className="w-12 h-12 bg-yellow-500 rounded-2xl flex items-center justify-center text-black"><LayoutGrid size={24}/></div><div><h1 className="text-2xl md:text-3xl font-black text-white">Panel Management</h1><p className="text-slate-500 text-sm mt-1">Interactive electrical panel map, live status and complete panel information</p></div></div><div className="flex flex-wrap gap-2"><button onClick={load} className="p-3 bg-white/5 border border-white/10 rounded-xl"><RefreshCw size={18} className={loading?"animate-spin":""}/></button><button onClick={()=>navigate("/panel-history")} className="flex items-center gap-2 px-5 py-3 bg-white/5 border border-white/10 rounded-xl text-xs font-black uppercase"><Archive size={17}/> Panel History</button><button onClick={()=>navigate("/add-panel")} className="flex items-center gap-2 px-5 py-3 bg-yellow-500 text-black rounded-xl text-xs font-black uppercase"><Plus size={17}/> Add New Panel</button></div></div>
    <div className="grid grid-cols-2 md:grid-cols-5 gap-3">{["total","live","off","maintenance","affected"].map(key=>{const cfg=STATUS[key]||STATUS.unknown,Icon=key==="total"?LayoutGrid:cfg.icon;return <button key={key} onClick={()=>setStatusFilter(key==="total"?"all":key)} className={`text-left p-4 rounded-2xl border ${statusFilter===(key==="total"?"all":key)?`${cfg.bg} ${cfg.border}`:"bg-[#020617] border-white/5"}`}><p className="text-[9px] uppercase tracking-widest text-slate-500 font-black">{key}</p><p className={`text-3xl font-black mt-2 ${cfg.color}`}>{counts[key]}</p><Icon size={17} className={`mt-2 ${cfg.color}`}/></button>})}</div>
    <section className="w-full bg-[#020617] border border-white/5 rounded-[2rem] overflow-hidden"><div className="px-5 py-4 border-b border-white/5 flex flex-col sm:flex-row sm:items-center justify-between gap-3"><div><h2 className="text-white font-black text-sm uppercase">Panel Location Map</h2><p className="text-slate-500 text-[10px] mt-1">Full factory map. All saved panel types and statuses remain visible here; filters affect only the list below.</p></div><div className="flex flex-wrap gap-2 text-[9px] font-black uppercase">{Object.entries(STATUS).slice(0,4).map(([k,c])=><span key={k} className="flex items-center gap-1.5 text-slate-400"><i className={`w-2.5 h-2.5 rounded-full ${c.dot}`}/>{c.label}</span>)}</div></div>
      <div className="relative w-full aspect-[2312/1643] min-h-[420px] bg-slate-950 overflow-hidden"><img src={factoryMap} alt="Factory electrical panel map" className="absolute inset-0 w-full h-full object-contain opacity-90"/>{loading&&<div className="absolute inset-0 flex items-center justify-center bg-slate-950/50"><RefreshCw size={30} className="animate-spin text-yellow-500"/></div>}{!loading&&panels.length===0&&<div className="absolute inset-0 flex items-center justify-center"><div className="text-center"><MapPin size={38} className="mx-auto text-slate-600 mb-2"/><p className="text-slate-500 font-black text-xs uppercase">No panels saved yet</p></div></div>}{!loading&&panels.map(panel=>{const cfg=STATUS[statusOf(panel)]||STATUS.unknown;return <button key={panel.id} type="button" title={`${panel.panel_code||panel.panel_name||"Panel"} — ${cfg.label}`} onClick={()=>setSelectedPanel(panel)} className="absolute -translate-x-1/2 -translate-y-1/2 group z-10 focus:outline-none" style={{left:`${Math.min(100,Math.max(0,Number(panel.x_position??50)))}%`,top:`${Math.min(100,Math.max(0,Number(panel.y_position??50)))}%`}}><span className={`flex items-center justify-center w-8 h-8 md:w-10 md:h-10 rounded-full ${cfg.dot} border-2 border-white shadow-xl shadow-black/60 group-hover:scale-125 transition-transform`}><MapPin size={16} className="text-slate-950"/></span><span className="mt-1 block max-w-32 truncate rounded-md bg-slate-950/90 border border-white/10 px-1.5 py-0.5 text-[8px] md:text-[9px] font-black text-white">{panel.panel_code||panel.panel_name||"Panel"}</span></button>})}</div>
    </section>
    <div className="bg-[#020617] border border-white/5 rounded-2xl p-4 flex flex-col sm:flex-row gap-3"><div className="relative flex-1"><Search size={16} className="absolute left-4 top-1/2 -translate-y-1/2 text-slate-500"/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search panels..." className="w-full bg-white/5 border border-white/10 rounded-xl pl-11 pr-4 py-3 text-sm text-white outline-none"/></div><select value={statusFilter} onChange={e=>setStatusFilter(e.target.value)} className="bg-white/5 border border-white/10 rounded-xl px-4 py-3 text-xs text-white"><option value="all">All Statuses</option><option value="live">Live</option><option value="off">Off</option><option value="maintenance">Maintenance</option><option value="affected">Affected</option></select></div>
    <div className="bg-[#020617] border border-white/5 rounded-[2rem] overflow-hidden"><div className="px-5 py-4 border-b border-white/5"><h2 className="text-white font-black text-sm uppercase">All Electrical Panels</h2><p className="text-slate-500 text-[10px] mt-1">Showing {filtered.length} of {panels.length} panels</p></div>{loading?<div className="min-h-[300px] flex items-center justify-center"><RefreshCw size={30} className="animate-spin text-yellow-500"/></div>:filtered.length===0?<div className="min-h-[300px] flex items-center justify-center text-slate-500 font-black text-xs uppercase">No panels found</div>:<div className="overflow-x-auto"><table className="w-full min-w-[1000px]"><thead><tr className="border-b border-white/5">{["Panel","Type","Location","Status","Electrical","Actions"].map(h=><th key={h} className="text-left px-5 py-4 text-[9px] uppercase tracking-widest text-slate-500">{h}</th>)}</tr></thead><tbody>{filtered.map(panel=>{const cfg=STATUS[statusOf(panel)]||STATUS.unknown,Icon=cfg.icon;return <tr key={panel.id} onClick={()=>setSelectedPanel(panel)} className="border-b border-white/[0.04] hover:bg-white/[0.025] cursor-pointer"><td className="px-5 py-4"><p className="text-white text-sm font-black">{panel.panel_name||"Unnamed Panel"}</p><p className="text-yellow-500 text-[10px] font-black mt-1">{panel.panel_code||"N/A"}</p></td><td className="px-5 py-4 text-xs text-slate-300">{panel.panel_type||"N/A"}</td><td className="px-5 py-4"><p className="text-white text-xs">{panel.area||"N/A"}</p><p className="text-slate-500 text-[10px]">{panel.location||"No location"}</p></td><td className="px-5 py-4"><div className={`inline-flex items-center gap-2 px-3 py-2 rounded-full text-[9px] uppercase font-black ${cfg.bg} ${cfg.color} border ${cfg.border}`}><Icon size={12}/>{cfg.label}</div><select onClick={e=>e.stopPropagation()} value={panel.status||"live"} disabled={updating===panel.id} onChange={e=>changeStatus(panel,e.target.value)} className="block mt-2 bg-white/5 border border-white/10 rounded-lg px-2 py-2 text-[10px] text-white"><option value="live">Live</option><option value="off">Off</option><option value="maintenance">Maintenance</option></select></td><td className="px-5 py-4"><p className="text-white text-xs">{panel.voltage||"N/A"}</p><p className="text-slate-500 text-[10px]">{panel.rated_current||"N/A"}</p></td><td className="px-5 py-4"><div className="flex gap-2"><button onClick={e=>{e.stopPropagation();navigate(`/add-panel/${panel.id}`)}} className="w-9 h-9 bg-yellow-500/10 text-yellow-400 rounded-xl flex items-center justify-center"><Pencil size={15}/></button><button onClick={e=>{e.stopPropagation();archive(panel)}} disabled={updating===panel.id} className="w-9 h-9 bg-red-500/10 text-red-400 rounded-xl flex items-center justify-center"><Trash2 size={15}/></button><button onClick={e=>{e.stopPropagation();setSelectedPanel(panel)}} className="w-9 h-9 bg-blue-500/10 text-blue-400 rounded-xl flex items-center justify-center"><Eye size={15}/></button></div></td></tr>})}</tbody></table></div>}</div>
    {selectedPanel&&(()=>{const cfg=STATUS[statusOf(selectedPanel)]||STATUS.unknown,Icon=cfg.icon;return <div className="fixed inset-0 z-[100] bg-black/70 backdrop-blur-sm p-3 sm:p-6 flex items-center justify-center" onMouseDown={e=>e.target===e.currentTarget&&setSelectedPanel(null)}><div className="w-full max-w-5xl max-h-[92vh] overflow-y-auto rounded-[2rem] border border-white/10 bg-[#020617] shadow-2xl"><div className="sticky top-0 z-10 flex items-center justify-between gap-3 p-5 border-b border-white/5 bg-[#020617]/95 backdrop-blur"><div><div className="flex items-center gap-2"><Info size={18} className="text-yellow-400"/><h2 className="text-xl font-black text-white">{val(selectedPanel,"panel_name")}</h2></div><p className="text-yellow-500 text-xs font-black mt-1">{val(selectedPanel,"panel_code")}</p></div><button onClick={()=>setSelectedPanel(null)} className="w-10 h-10 rounded-xl bg-white/5 border border-white/10 flex items-center justify-center"><X size={18}/></button></div><div className="p-5 space-y-5"><div className="flex flex-wrap items-center justify-between gap-3"><div className="flex items-center gap-2"><span className={`inline-flex items-center gap-2 px-4 py-2 rounded-full ${cfg.bg} ${cfg.color} border ${cfg.border} text-xs font-black uppercase`}><Icon size={14}/>{cfg.label}</span><span className="text-xs text-slate-500">{val(selectedPanel,"area")} · {val(selectedPanel,"location")}</span></div><select value={selectedPanel.status||"live"} disabled={updating===selectedPanel.id} onChange={e=>changeStatus(selectedPanel,e.target.value)} className="bg-white/5 border border-white/10 rounded-xl px-4 py-2 text-xs text-white"><option value="live">Live</option><option value="off">Off</option><option value="maintenance">Maintenance</option></select></div><div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-3">{DETAIL_FIELDS.map(([label,key])=><div key={key} className="rounded-xl bg-white/[0.035] border border-white/5 p-3"><p className="text-[8px] uppercase tracking-widest text-slate-500 font-black">{label}</p><p className="text-sm text-white font-bold mt-1 break-words">{val(selectedPanel,key)}</p></div>)}</div><div className="grid md:grid-cols-2 gap-4"><div className="rounded-xl bg-white/[0.035] border border-white/5 p-4"><p className="text-[9px] uppercase tracking-widest text-slate-500 font-black">Description</p><p className="text-sm text-slate-300 mt-2 whitespace-pre-wrap">{val(selectedPanel,"description")}</p></div><div className="rounded-xl bg-white/[0.035] border border-white/5 p-4"><p className="text-[9px] uppercase tracking-widest text-slate-500 font-black">Notes</p><p className="text-sm text-slate-300 mt-2 whitespace-pre-wrap">{val(selectedPanel,"notes")}</p></div></div><div className="flex flex-col sm:flex-row justify-end gap-2"><button onClick={()=>{setSelectedPanel(null);navigate(`/add-panel/${selectedPanel.id}`)}} className="px-5 py-3 rounded-xl bg-yellow-500 text-black text-xs font-black"><Pencil size={14} className="inline mr-2"/>Edit Panel</button><button onClick={()=>archive(selectedPanel)} className="px-5 py-3 rounded-xl bg-red-500/10 text-red-400 text-xs font-black"><Trash2 size={14} className="inline mr-2"/>Archive Panel</button></div></div></div></div>})()}
  </div>;
}
